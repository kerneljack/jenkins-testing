pipeline {
    agent any
    stages {
        stage('build') {
            steps {
                sh 'docker --version'
                sh 'hostname'
                sh 'python --version'
            }
        }
    }
}
